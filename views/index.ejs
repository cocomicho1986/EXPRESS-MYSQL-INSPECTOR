<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>MySQL Inspector (Simple)</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; max-width: 1000px; margin: 0 auto; }
    select, textarea, button { width: 100%; padding: 10px; margin: 8px 0; box-sizing: border-box; }
    textarea { height: 120px; font-family: monospace; }
    table { width: 100%; border-collapse: collapse; margin-top: 15px; }
    th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
    th { background-color: #f2f2f2; }
    .error { color: red; background: #ffe6e6; padding: 10px; border-radius: 4px; margin: 10px 0; }
    .success { color: green; margin: 10px 0; font-weight: bold; }
  </style>
</head>
<body>
  <h1>🔍 MySQL Inspector (Simple)</h1>
  <p>Consulta tus bases de datos con SQL</p>

  <form method="POST">
    <label>Base de datos:</label>
    <select name="database" required>
      <option value="">-- Selecciona una base --</option>
      <% databases.forEach(db => { %>
        <option value="<%= db %>" <%= (selectedDb === db) ? 'selected' : '' %>><%= db %></option>
      <% }) %>
    </select>

    <label>Consulta SQL (solo SELECT):</label>
    <textarea name="query" placeholder="Ej: SELECT * FROM tu_tabla LIMIT 10;"><%= query || '' %></textarea>

    <button type="submit">▶️ Ejecutar</button>
  </form>

  <% if (error) { %>
    <div class="error">❌ <%= error %></div>
  <% } %>

  <% if (results) { %>
    <div class="success">✅ <%= results.rowCount %> filas</div>
    <div style="max-height: 500px; overflow: auto;">
      <table>
        <thead>
          <tr>
            <% results.columns.forEach(col => { %>
              <th><%= col %></th>
            <% }) %>
          </tr>
        </thead>
        <tbody>
          <% results.results.forEach(row => { %>
            <tr>
              <% results.columns.forEach(col => {
                const val = row[col];
                if (val == null) { %>
                  <td><em>NULL</em></td>
                <% } else if (Buffer.isBuffer(val)) { %>
                  <td>[BLOB]</td>
                <% } else if (typeof val === 'object') { %>
                  <td><%= JSON.stringify(val) %></td>
                <% } else { %>
                  <td><%= String(val).replace(/</g, '&lt;').replace(/>/g, '&gt;') %></td>
                <% } %>
              <% }) %>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>
</body>
</html>